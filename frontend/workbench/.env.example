# ============================================
# 通用配置（所有环境通用）
# ============================================

# 项目运行模式：development / test / production
VITE_APP_ENV=development

# 项目名称
VITE_APP_NAME=My React App

# ============================================
# 接口配置（按环境区分）
# ============================================

# 后端接口基础地址
# 在 .env.development / .env.test / .env.production 中设置真实值
VITE_API_BASE_URL=http://127.0.0.1:5018

# ============================================
# 身份认证相关
# ============================================

# 用于请求头中的认证 token（仅示例）
VITE_AUTH_TOKEN_KEY=token

# ============================================
# 其他可选配置
# ============================================

# 启用 mock 数据（true/false）
VITE_USE_MOCK=false

# 是否开启调试日志
VITE_DEBUG=false

# 构建时是否开启 sourcemap（生产环境建议关闭）
VITE_BUILD_SOURCEMAP=false
